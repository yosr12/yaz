{% extends 'back.html.twig' %}

{% block title %}Edit Voyage{% endblock %}

{% block body %}
    <h1>Edit Voyage</h1>


    {{ include('voyage/_form.html.twig', {'button_label type="button" class="btn btn-danger"': 'Update' }) }}

    <a href="{{ path('voyage_index') }}"style="color:blue;">back to list</a>

    {{ include('voyage/_delete_form.html.twig') }}
{% endblock %}
