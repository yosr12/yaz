{% extends 'back.html.twig' %}
{%block body%}
<section id="main-content">
      <section class="wrapper">
        <!-- BASIC FORM ELELEMNTS -->
        <div class="row mt">
          


  <br> </br>
  <br> </br>
  <h2> <center>Liste des Maisons d'hote</h2>
  <div class="col-md-12">
            <div class="section-heading">
             <left> <a href="/hebergement/newMaison">Ajouter un Offre <i class="fa fa-angle-right"></i></a>
            </div>
          </div>
<table class="table table-striped">
    <thead>
      <tr>
        <th>Nom</th>
        <th>Adresse</th>
        <th>Prix</th>
        <th>Description</th>
        <th>Date Debut</th>
        <th>Date Fin</th>
        <th>Editer</th>
      </tr>
    </thead>
    <tbody>
    {% for maison in maisons %}
      <tr><td>
       {{ maison.nom }}
      </td>   <td>
        {{ maison.adresse}}
       </td> <td>
     {{ maison.price}}
       </td> <td>
     {{ maison.description }}
      </td>   <td>
        {{ maison.datedebut|date("m/d/y")}}
       </td> <td>
     {{ maison.datefin|date("m/d/y")}}
     <td>
       

     <a href="{{ path('updateMaison', {'id': maison.id}) }}"> Modifier</a>
     <a href="{{path('deleteMaison', {'id': maison.id}) }}"> Supprimer</a>
     </td>

     </tr> 
    </tbody>
    {% endfor %}
  </table>
    


{%endblock%}