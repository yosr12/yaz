{% extends 'base2.html.twig' %}

{% block body %}

<section id="main-content">
      <section class="wrapper">
        <!-- BASIC FORM ELELEMNTS -->
        <div class="row mt">
          <div class="col-lg-6 col-md-6 col-sm-6">
<div class="container">

  <h2> <center>Liste des réclamations</h2>

   <br> </br>  
  <th>Trier : </th>
  <a href="{{path('TrierReclamation2') }}"> ASC</a>   
  <a href="{{path('TrierReclamation') }}">  DESC</a>
<br> </br>   

  <table class="table table-striped">
    <thead>
      <tr>
        <th>Sujet</th>
        <th>Date</th>
        <th>Description</th>
        <th>Editer</th>
      </tr>
    </thead>
    <tbody>
    {% for Reclamation in Reclamations %}
      <tr><td>
       {{ Reclamation.sujet }}
      </td>   <td>
        {{ Reclamation.date}}
       </td> <td>
     {{ Reclamation.description}}
       </td> <td>

     <a href="{{path('showreclamationBack', {'id': Reclamation.id}) }}"> Consulter</a>
     <a href="{{path('deletereclamationBack', {'id': Reclamation.id}) }}"> Supprimer</a>

     </tr> 
    </tbody>
    {% endfor %}
  </table>   


</div>
</div>
</div>
</section>
{% endblock %}

